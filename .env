# SAM Mask Service Configuration

# API Security (Optional - leave empty for no authentication)
# Generate a strong key: openssl rand -hex 32
SAM_API_KEY=your-secret-api-key-here

# Example usage with API key:
# curl -X POST http://localhost:8739/create_mask \
#   -H "X-API-Key: your-secret-api-key-here" \
#   -H "Content-Type: application/json" \
#   -d '{"image_url": "https://example.com/image.jpg"}'
#
# Or with query parameter:
# curl -X POST "http://localhost:8739/create_mask?api_key=your-secret-api-key-here" \
#   -H "Content-Type: application/json" \
#   -d '{"image_url": "https://example.com/image.jpg"}'