# SAM Mask API - Environment Variables

# API Key for authentication (optional)
SAM_API_KEY=your-secret-api-key-here

# Redis connection for job queue
# Format: redis://[username]:[password]@host:port/db_number
# Use DB 1 to avoid conflicts with Laravel (which typically uses DB 0)
# Example with password only: redis://:password@host:6379/1
# Example with username and password: redis://username:password@host:6379/1
REDIS_URL=redis://:your-redis-password@your-redis-host:6379/1
